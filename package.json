{
  "name": "travel-advisor-bundle",
  "version": "1.0.0",
  "description": "Welcome to **Travel Advisor** - your one-stop destination for travel recommendations! ✈️🌍",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-frontend": "cd frontend && npm start",
    "start-backend": "cd backend && node --max-old-space-size=4096 server.js",
    "start": "npm-run-all --parallel start-frontend start-backend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "env": "^0.0.2",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
